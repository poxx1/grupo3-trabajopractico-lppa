<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link rel="preload" href="~/Content/Normalize.css" as="style">
    <link href="~/Content/Site.css" rel="stylesheet" />
</head>
<body>
    <div id="contenedor">
        @*<div id="cabecera">
                <div id="buscador">
                    <form action="#" method="post">
                        Buscar &nbsp;
                        <input name="busqueda" type="text" />
                    </form>
                </div>
            </div>
            <div>
                <h1> Bienvenido a la tienda web del Grupo 3 </h1>
            </div>*@
        <div class="nav-bg">
            <div id="menu">
                <ul id="menu_principal">
                    <li><a href="/" class="Compra">Nueva Compra</a></li>
                    @*<li><a href="@Url.Action("Index","Category")">Categorias</a></li>
                        <li><a href="@Url.Action("Index","Product")">Productos</a></li>*@
                </ul>
                <ul id="menu_secundario">
                    <li>
                        @if (!User.Identity.IsAuthenticated)
                        {
            <a href="@Url.Action("LogIn", "Auth")">Iniciar Sesion</a>}
                    </li>
                    <li class="AdminPanel">
                        @if (User.Identity.IsAuthenticated)
                        {
            <a href="@Url.Action("Index","Admin")" class="AdminPanel">Panel Admin</a>
                            <a href="@Url.Action("LogOut","Auth")" class="Logout">Log Out</a>}
                    </li>
                </ul>
            </div>
        </div>
        <div id="contenido">
            <div id="principal">
                @RenderBody() @*Donde se van a anexar todas las paginas parciales que voy a utilizar*@
            </div>
        </div>
        <div id="pie">
            <p> Universidad Abierta Interamericana</p>
            <p> Lenguajes de administración para la administración</p>
            <p> Marketplace Grupo 3 - 2021</p>
        </div>
    </div>
</body>
</html>
